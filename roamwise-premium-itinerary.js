/* RoamWise Cinematic Itinerary Engine v4.0
 * Classic remains the source of truth. Cinematic is progressive enhancement only.
 * v4: inline Classic action, structured day data, coordinate-first maps/elevation,
 * premium PDF pack integration, and readable editorial journey cards.
 */
(function(){
'use strict';
const VERSION='4.0.0';
const state={mode:'classic',observer:null,lastFingerprint:'',renderToken:0,route:[],geoCache:{},carouselIndex:0,audio:null,pdfHooked:false};
const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>Array.from(r.querySelectorAll(s));
const esc=v=>String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]||c));
const clean=v=>String(v==null?'':v).replace(/\s+/g,' ').trim();
const norm=v=>clean(v).toLowerCase().normalize('NFKD').replace(/[^a-z0-9]+/g,' ').trim();
const clamp=(n,a,b)=>Math.min(b,Math.max(a,n));
function ensureStyles(){if($('#rwCineStyles'))return;const l=document.createElement('link');l.id='rwCineStyles';l.rel='stylesheet';l.href='roamwise-premium-itinerary.css?v=4.0.0';document.head.appendChild(l)}
function toast(msg){$('.rw-cine-toast')?.remove();const n=document.createElement('div');n.className='rw-cine-toast';n.textContent=msg;document.body.appendChild(n);setTimeout(()=>n.remove(),2600)}
function isPro(){try{if(window.RW_CINEMATIC_PREVIEW===true||sessionStorage.getItem('rw_cinematic_preview')==='1')return true;if(window.isPro===true||window.RW_PRO===true||window.proUnlocked===true)return true;if(localStorage.getItem('rwPro')==='1')return true;if(typeof window.rwIsPro==='function'&&window.rwIsPro())return true;if(typeof window.isPro==='function'&&window.isPro())return true;if(window.RW_USER?.isPro===true||window.RW?.user?.isPro===true)return true}catch(_){ }return false}
function openUpgrade(){toast('Cinematic Journey is included with RoamWise Pro.');try{if(typeof window.openPay==='function')return window.openPay()}catch(_){ }$('#heroProBtn')?.click()}
function formContext(){const last=window._lastItin||{},tags=$$('#tagsContainer .tag.on').map(x=>x.dataset.v||clean(x.textContent)).filter(Boolean),get=id=>$(id)?.value||'',sel=id=>$(id)?.selectedOptions?.[0]?.textContent?.trim()||get(id);return{destination:clean(get('#destInput')||last.name||last.destination||'Your trip'),origin:clean(get('#origin')||'Your city'),month:clean(sel('#month')||'Flexible'),days:Math.max(1,parseInt(get('#dur')||last.days?.length||7,10)||7),duration:clean(sel('#dur')||((last.days?.length||7)+' days')),style:clean(sel('#style')||'Balanced'),crowd:clean(sel('#crowd')||'Flexible'),transport:clean(sel('#tmode')||'Mixed'),budgetExact:clean(get('#budgetExact')),budget:clean($('#budgetDisplay')?.textContent||get('#budgetDisplay')||'Flexible'),tags}}
function stringish(v){if(v==null)return'';if(typeof v==='string'||typeof v==='number')return clean(v);if(Array.isArray(v))return clean(v.map(stringish).filter(Boolean).join(' · '));return''}
function pick(o,keys){for(const k of keys){const v=o?.[k],s=stringish(v);if(s)return s}return''}
function stripLabel(s,label){return clean(String(s||'').replace(new RegExp('^\\s*'+label+'\\s*[:\\-–—]?\\s*','i'),''))}
function parseLabeled(raw){const out={};const labels=[['morning','morning|am'],['afternoon','afternoon|pm'],['evening','evening|eve'],['tip','tip|field intel|local tip|note'],['stay','stay|hotel|accommodation'],['food','food|meal|eat'],['budget','budget|cost|spend']];for(const [key,pat] of labels){const re=new RegExp('(?:^|[·|\\n])\\s*(?:'+pat+')\\s*[:\\-]\\s*([^·|\\n]+)','i'),m=String(raw||'').match(re);if(m)out[key]=clean(m[1])}return out}
function structuredDay(d,i){const n=Number(d?.day||d?.n||d?.dayNumber||i+1)||i+1;let title=pick(d,['title','name','headline','place','location'])||`Day ${n}`;title=stripLabel(title,'Day\\s*\\d+');const raw=stringish(d?.source)||stringish(d?.body)||stringish(d?.description)||'';const lab=parseLabeled(raw);const day={n,title:clean(title)||`Day ${n}`,morning:pick(d,['morning','am','breakfast'])||lab.morning,afternoon:pick(d,['afternoon','pm','daytime'])||lab.afternoon,evening:pick(d,['evening','night'])||lab.evening,tip:pick(d,['tip','note','intel','localTip','fieldIntel'])||lab.tip,stay:pick(d,['stay','hotel','accommodation','lodging'])||lab.stay,food:pick(d,['food','meals','meal','eat'])||lab.food,budget:pick(d,['budget','cost','spend'])||lab.budget,place:pick(d,['place','location','city','stop']),full:raw};
  if(!day.morning&&!day.afternoon&&!day.evening){const text=clean(raw.replace(/\b(day|title)\s*:\s*[^·|]+[·|]?/ig,''));if(text)day.afternoon=text.slice(0,900)}
  return day;
}
function daysFromState(ctx){const it=window._lastItin;if(!it||!Array.isArray(it.days)||!it.days.length)return[];const n=norm(it.name||it.destination),d=norm(ctx.destination);if(n&&d&&n!==d&&!n.includes(d)&&!d.includes(n))return[];return it.days.map(structuredDay).filter(x=>x.title||x.morning||x.afternoon||x.evening||x.full).slice(0,30)}
function domDayBlocks(root){const markers=$$('h1,h2,h3,h4,strong,b,[data-day],[class*="day-title"]',root).filter(x=>/\bDay\s*\d+\b/i.test(clean(x.textContent)));const seen=new Set(),out=[];for(const m of markers){const mm=clean(m.textContent).match(/\bDay\s*(\d+)\b/i);if(!mm||seen.has(mm[1]))continue;let b=m;while(b&&b!==root&&clean(b.innerText||b.textContent).length<100)b=b.parentElement;if(!b)continue;const raw=clean(b.innerText||b.textContent);if(raw.length<60)continue;seen.add(mm[1]);out.push(structuredDay({day:Number(mm[1]),title:clean(m.textContent),source:raw},out.length))}return out.sort((a,b)=>a.n-b.n).slice(0,30)}
function extractDays(ctx){const a=daysFromState(ctx);if(a.length)return a;const r=$('#results');return r?domDayBlocks(r):[]}
function hasClassic(){const r=$('#results');if(!r)return false;const ctx=formContext(),days=extractDays(ctx);return days.length>0&&clean(r.innerText||r.textContent).length>100}
function fingerprint(){const r=$('#results'),c=formContext();return c.destination+'|'+c.days+'|'+clean(r?.innerText||'').slice(0,1400)}
function removeOldLauncher(){$('#rwCineResultLauncher')?.remove()}
function findClassicActionHost(){const r=$('#results');if(!r)return null;const controls=$$('button,a',r);const pdf=controls.find(x=>/premium\s*pdf|pdf itinerary|download.*pdf/i.test(clean(x.textContent))||/openPdfFlow/.test(x.getAttribute('onclick')||''));if(pdf)return{host:pdf.parentElement||r,before:pdf};const likely=controls.filter(x=>/save|share|offline|compare|map|book|download/i.test(clean(x.textContent)));if(likely.length)return{host:likely[likely.length-1].parentElement||r,before:null};return{host:r,before:r.firstElementChild};}
function ensureInlineButton(){removeOldLauncher();const old=$('#rwCineInlineBtn');if(!hasClassic()){old?.remove();if(state.mode==='cinematic')setMode('classic',true);return}if(old)return syncInlineButton();const spot=findClassicActionHost();if(!spot)return;const b=document.createElement('button');b.id='rwCineInlineBtn';b.type='button';b.className='tact rw-cine-inline-btn';b.innerHTML='<span class="rw-cine-spark">✦</span><span>Cinematic</span><small>PRO</small>';b.addEventListener('click',()=>setMode('cinematic'));if(spot.before&&spot.before.parentElement===spot.host)spot.host.insertBefore(b,spot.before);else spot.host.appendChild(b);syncInlineButton()}
function syncInlineButton(){const b=$('#rwCineInlineBtn');if(!b)return;b.classList.toggle('on',state.mode==='cinematic');b.setAttribute('aria-pressed',String(state.mode==='cinematic'))}
function setMode(mode,silent){const r=$('#results');if(mode==='cinematic'){if(!isPro())return openUpgrade();if(!hasClassic()){if(!silent)toast('Generate the Classic itinerary first.');return false}if(!renderCinematic()){if(!silent)toast('Classic itinerary could not be read yet.');return false}state.mode='cinematic';r?.classList.add('rw-cine-classic-hidden');syncInlineButton();return true}state.mode='classic';r?.classList.remove('rw-cine-classic-hidden');$('#rwCinematicMount')?.remove();state.mode='classic';state.renderToken++;state.route=[];syncInlineButton();if(!silent)r?.scrollIntoView({behavior:'smooth',block:'start'});return true}
function safeStops(dest,days){try{if(typeof window.rwDeriveStops==='function'){const got=window.rwDeriveStops(dest);if(Array.isArray(got)&&got.length)return got.map((s,i)=>({day:Number(s.day||i+1),name:clean(s.name||s.place||dest),note:clean(s.note||s.title||'')}))}}catch(_){ }return days.map(d=>({day:d.n,name:clean(d.place||placeFromTitle(d.title)||dest),note:d.title}))}
function placeFromTitle(t){const a=clean(t).split(/→|->|\bto\b/i).filter(Boolean);let x=a[a.length-1]||t;x=x.replace(/^(bonus exploration|free day|buffer day|rest day)$/i,'');return clean(x)}
function dbCenter(dest){try{const d=(window.DB||[]).find(x=>norm(x.name)===norm(dest)||norm(dest).includes(norm(x.name))||norm(x.name).includes(norm(dest)));if(d&&Number.isFinite(+d.lat)&&Number.isFinite(+d.lon))return{lat:+d.lat,lng:+d.lon,name:d.name,elevation:Number(d.elevation||0)||null,source:'roamwise-db'}}catch(_){ }return null}
function timeout(p,ms=4500){return Promise.race([Promise.resolve(p),new Promise((_,rej)=>setTimeout(()=>rej(new Error('timeout')),ms))])}
async function appGeocode(q){if(!q)return null;const k=norm(q);if(state.geoCache[k])return state.geoCache[k];try{if(typeof window.gcode==='function'){const g=await timeout(window.gcode(q),4200);if(g&&Number.isFinite(+g.lat)&&Number.isFinite(+g.lon)){const o={lat:+g.lat,lng:+g.lon,name:g.name||q,elevation:Number(g.elevation||0)||null,source:'roamwise-geocoder'};state.geoCache[k]=o;return o}}}catch(_){ }try{const c=new AbortController(),tm=setTimeout(()=>c.abort(),4200),u='https://geocoding-api.open-meteo.com/v1/search?name='+encodeURIComponent(q)+'&count=1&language=en&format=json',r=await fetch(u,{cache:'force-cache',signal:c.signal});clearTimeout(tm);if(r.ok){const g=(await r.json())?.results?.[0];if(g){const o={lat:+g.latitude,lng:+g.longitude,name:g.name||q,elevation:Number(g.elevation||0)||null,source:'open-meteo'};state.geoCache[k]=o;return o}}}catch(_){ }return null}
function genericStop(name){return /^(bonus exploration|free day|buffer|buffer day|rest day|arrival|departure|explore|local day)$/i.test(clean(name))}
async function resolveRoute(stops,ctx,token){const center=dbCenter(ctx.destination)||await appGeocode(ctx.destination);let bounded=null;try{if(typeof window.rwGeocodeStopsNear==='function')bounded=await timeout(window.rwGeocodeStopsNear(ctx.destination,stops),5200)}catch(_){ }const pinMap=new Map((bounded?.pins||[]).map(p=>[Number(p.day),{lat:+p.lat,lng:+p.lon,name:p.name,elevation:Number(p.elevation||0)||null,source:'roamwise-validated'}]));const out=[];let prev=center;for(const s of stops){if(token!==state.renderToken)return[];let geo=pinMap.get(Number(s.day))||null;if(!geo&&!genericStop(s.name)){geo=await appGeocode(s.name+', '+ctx.destination)||await appGeocode(s.name)}if(!geo)geo=prev||center;if(geo)prev=geo;out.push({...s,geo:geo||null})}return out}
function hasElevation(v){return v!==null&&v!==undefined&&v!==''&&Number.isFinite(Number(v))}
async function elevations(points){const need=points.filter(p=>p.geo&&!hasElevation(p.geo.elevation));if(need.length){try{const lats=need.map(p=>p.geo.lat).join(','),lngs=need.map(p=>p.geo.lng).join(','),c=new AbortController(),tm=setTimeout(()=>c.abort(),5000),r=await fetch('https://api.open-meteo.com/v1/elevation?latitude='+encodeURIComponent(lats)+'&longitude='+encodeURIComponent(lngs),{cache:'force-cache',signal:c.signal});clearTimeout(tm);if(r.ok){const vals=(await r.json())?.elevation||[];need.forEach((p,i)=>{if(Number.isFinite(+vals[i]))p.geo.elevation=+vals[i]})}}catch(_){ }}return points}
function photoFor(place,dest){const db=window.RW_PHOTOS_DATA||{},q=[norm(place),norm(dest)].filter(Boolean);let k=Object.keys(db).find(x=>q.includes(norm(x)));if(!k)k=Object.keys(db).find(x=>q.some(v=>v&&((norm(x).includes(v))||(v.includes(norm(x))))));return k?db[k]:''}
function heroPhoto(ctx,route){return photoFor(route?.[0]?.name,ctx.destination)||photoFor(ctx.destination,ctx.destination)}
function theme(ctx){const d=norm(ctx.destination);if(/kedarnath|badrinath|varanasi|pilgrim|temple|rishikesh/.test(d))return'pilgrim';if(/everest|annapurna|kilimanjaro|trek|expedition|base camp|chadar|hampta/.test(d))return'expedition';if(/meghalaya|tawang|sikkim|northeast|arunachal/.test(d))return'frontier';if(/goa|bali|maldives|island|coast|beach/.test(d))return'coast';return'crimson'}
function phaseHTML(icon,label,value){if(!value)return'';return`<div class="rw-v4-phase"><span>${icon}</span><div><b>${label}</b><p>${esc(value)}</p></div></div>`}
function infoPill(label,value){return value?`<span class="rw-v4-pill"><b>${esc(label)}</b>${esc(value)}</span>`:''}
function dayCard(d,ctx,i){const pic=photoFor(d.place||placeFromTitle(d.title),ctx.destination)||photoFor(ctx.destination,ctx.destination);return`<article class="rw-v4-day" data-rw-day="${d.n}" data-rw-index="${i}">${pic?`<div class="rw-v4-day-photo" style="background-image:url('${esc(pic)}')"><span>Day ${d.n}</span></div>`:`<div class="rw-v4-day-photo rw-v4-photo-fallback"><span>Day ${d.n}</span></div>`}<div class="rw-v4-day-body"><div class="rw-v4-day-head"><div><small>DAY ${d.n}</small><h3>${esc(d.title)}</h3></div><button class="rw-v4-icon-btn" data-rw-day-map="${d.n}" title="Day map">⌖</button></div><div class="rw-v4-phases">${phaseHTML('☀','Morning',d.morning)}${phaseHTML('◐','Afternoon',d.afternoon)}${phaseHTML('✦','Evening',d.evening)}${(!d.morning&&!d.afternoon&&!d.evening)?phaseHTML('◆','Plan',d.full):''}</div>${d.tip?`<div class="rw-v4-tip"><b>Local note</b><span>${esc(d.tip)}</span></div>`:''}<div class="rw-v4-day-tools">${infoPill('Stay',d.stay)}${infoPill('Food',d.food)}${infoPill('Budget',d.budget)}<button data-rw-details="${d.n}">Full details</button></div><div class="rw-v4-full" data-rw-full="${d.n}" hidden>${esc(d.full||[d.morning,d.afternoon,d.evening,d.tip].filter(Boolean).join(' · '))}</div><div class="rw-v4-mini-map" data-rw-mini-map="${d.n}" hidden></div></div></article>`}
function navHTML(days){return`<div class="rw-v4-day-nav"><div><small>Day by day</small><strong><span id="rwV4Current">1</span> / ${days.length}</strong></div><div class="rw-v4-day-dots">${days.map((d,i)=>`<button data-rw-jump="${i}" class="${i===0?'on':''}">D${d.n}</button>`).join('')}</div><div class="rw-v4-nav-arrows"><button data-rw-step="-1">‹</button><button data-rw-step="1">›</button></div></div>`}
function renderCinematic(){const r=$('#results'),ctx=formContext(),days=extractDays(ctx);if(!r||!days.length)return false;ensureStyles();$('#rwCinematicMount')?.remove();const token=++state.renderToken,stops=safeStops(ctx.destination,days),t=theme(ctx),photo=heroPhoto(ctx,stops);const mount=document.createElement('section');mount.id='rwCinematicMount';mount.className='rw-cinematic-v4 theme-'+t;mount.innerHTML=`<div class="rw-v4-hero">${photo?`<div class="rw-v4-hero-photo" style="background-image:url('${esc(photo)}')"></div>`:''}<div class="rw-v4-hero-shade"></div><div class="rw-v4-hero-copy"><div class="rw-v4-brand">ROAMWISE <span>✦ CINEMATIC</span></div><h1>${esc(ctx.destination)}</h1><p>${ctx.days}-day journey · ${esc(ctx.month)} · ${esc(ctx.style)}</p><div class="rw-v4-facts">${infoPill('From',ctx.origin)}${infoPill('Crowds',ctx.crowd)}${infoPill('Move',ctx.transport)}${ctx.tags.slice(0,2).map(x=>infoPill('',x)).join('')}</div><div class="rw-v4-hero-actions"><button data-rw-v4-action="classic">← Classic</button><button data-rw-v4-action="map">⌖ Route map</button><button data-rw-v4-action="pdf">⇩ Premium PDF pack</button><button data-rw-v4-action="share">↗ Share</button></div></div></div><section class="rw-v4-section rw-v4-overview"><div class="rw-v4-section-head"><div><small>JOURNEY AT A GLANCE</small><h2>See the route. Feel the terrain.</h2><p>A map-first overview built from RoamWise route points. External map tiles are optional, not required.</p></div></div><div class="rw-v4-atlas-grid"><div class="rw-v4-panel"><div class="rw-v4-panel-head"><div><b>Route atlas</b><span>Geographic path + daily stops</span></div><span class="rw-v4-status" id="rwV4MapStatus">Building route…</span></div><div id="rwV4Atlas" class="rw-v4-atlas"><div class="rw-v4-skeleton">${routeStorySvg(stops)}</div></div></div><div class="rw-v4-panel"><div class="rw-v4-panel-head"><div><b>Elevation</b><span>Terrain profile along this trip</span></div><span class="rw-v4-status" id="rwV4ElevStatus">Resolving…</span></div><div id="rwV4Elevation" class="rw-v4-elevation"><div class="rw-v4-elev-placeholder">Terrain data will appear here without blocking the itinerary.</div></div></div></div></section><section class="rw-v4-section rw-v4-days"><div class="rw-v4-section-head"><div><small>YOUR DAYS</small><h2>A trip you can actually read.</h2><p>Swipe day by day. The important moments stay visible; deeper detail is one tap away.</p></div></div>${navHTML(days)}<div id="rwV4Carousel" class="rw-v4-carousel">${days.map((d,i)=>dayCard(d,ctx,i)).join('')}</div></section><div class="rw-cine-toast" hidden></div>`;r.after(mount);wireCinematic(mount,days,ctx);resolveRoute(stops,ctx,token).then(x=>{if(token!==state.renderToken)return;state.route=x;elevations(x).then(y=>{if(token!==state.renderToken)return;state.route=y;paintAtlas(y,ctx);paintElevation(y);paintVisibleMiniMaps()})});return true}
function routeStorySvg(stops){const n=Math.max(stops.length,2),pts=stops.map((s,i)=>{const x=40+(i/(n-1))*520,y=110+Math.sin(i*1.45)*48;return{x,y,s}});return`<svg viewBox="0 0 600 240" aria-label="Route loading"><path d="${pts.map((p,i)=>(i?'L':'M')+p.x+' '+p.y).join(' ')}" fill="none" stroke="rgba(255,255,255,.12)" stroke-width="12" stroke-linecap="round"/><path d="${pts.map((p,i)=>(i?'L':'M')+p.x+' '+p.y).join(' ')}" fill="none" stroke="var(--rw-accent)" stroke-width="3" stroke-dasharray="8 8"/>${pts.map((p,i)=>`<circle cx="${p.x}" cy="${p.y}" r="7" fill="#fff" stroke="var(--rw-accent)" stroke-width="3"/><text x="${p.x}" y="${p.y+28}" text-anchor="middle" fill="#cbd2dc" font-size="10">D${p.s.day}</text>`).join('')}</svg>`}
function project(points){const valid=points.filter(p=>p.geo);if(!valid.length)return null;const lats=valid.map(p=>p.geo.lat),lngs=valid.map(p=>p.geo.lng),minLat=Math.min(...lats),maxLat=Math.max(...lats),minLng=Math.min(...lngs),maxLng=Math.max(...lngs),dx=Math.max(maxLng-minLng,.03),dy=Math.max(maxLat-minLat,.03);return valid.map(p=>({...p,x:52+((p.geo.lng-minLng)/dx)*496,y:208-((p.geo.lat-minLat)/dy)*158}))}
function paintAtlas(points,ctx){const host=$('#rwV4Atlas'),status=$('#rwV4MapStatus');if(!host)return;const pp=project(points);if(!pp||!pp.length){if(status)status.textContent='Route story';host.innerHTML=routeStorySvg(points.length?points:safeStops(ctx.destination,extractDays(ctx)));return}const path=pp.map((p,i)=>(i?'L':'M')+p.x.toFixed(1)+' '+p.y.toFixed(1)).join(' ');host.innerHTML=`<svg viewBox="0 0 600 260" role="img" aria-label="${esc(ctx.destination)} route map"><defs><pattern id="rwGrid" width="30" height="30" patternUnits="userSpaceOnUse"><path d="M30 0H0V30" fill="none" stroke="rgba(255,255,255,.045)"/></pattern></defs><rect width="600" height="260" rx="18" fill="#111720"/><rect width="600" height="260" rx="18" fill="url(#rwGrid)"/><path d="${path}" fill="none" stroke="rgba(255,255,255,.10)" stroke-width="15" stroke-linecap="round"/><path d="${path}" fill="none" stroke="var(--rw-accent)" stroke-width="4" stroke-linecap="round" stroke-linejoin="round"/>${pp.map((p,i)=>`<g><circle cx="${p.x}" cy="${p.y}" r="10" fill="#f7f8fa" stroke="var(--rw-accent)" stroke-width="4"/><text x="${p.x}" y="${p.y+3}" text-anchor="middle" fill="#0b0e13" font-size="9" font-weight="900">${p.day}</text><text x="${p.x}" y="${p.y+(i%2?31:-19)}" text-anchor="middle" fill="#f7f8fa" stroke="#111720" stroke-width="4" paint-order="stroke" font-size="10" font-weight="700">${esc(shortName(p.name))}</text></g>`).join('')}</svg><div class="rw-v4-map-links"><button data-rw-open-route>Open real map ↗</button><span>${pp.length} mapped stops</span></div>`;if(status)status.textContent='Mapped';$('[data-rw-open-route]',host)?.addEventListener('click',()=>{const p=pp[Math.floor(pp.length/2)];window.open('https://www.openstreetmap.org/?mlat='+p.geo.lat+'&mlon='+p.geo.lng+'#map=8/'+p.geo.lat+'/'+p.geo.lng,'_blank','noopener')})}
function shortName(s){const x=clean(s);return x.length>22?x.slice(0,20)+'…':x}
function paintElevation(points){const host=$('#rwV4Elevation'),status=$('#rwV4ElevStatus');if(!host)return;const v=points.filter(p=>p.geo&&hasElevation(p.geo.elevation));if(v.length<2){if(status)status.textContent='Terrain optional';host.innerHTML='<div class="rw-v4-elev-placeholder compact"><b>Route is ready</b><span>Live terrain data is unavailable right now. Map and daily itinerary remain complete.</span></div>';return}const vals=v.map(p=>+p.geo.elevation),min=Math.min(...vals),max=Math.max(...vals),span=Math.max(max-min,1),pts=v.map((p,i)=>({p,x:42+(i/Math.max(v.length-1,1))*456,y:202-((p.geo.elevation-min)/span)*145})),line=pts.map((a,i)=>(i?'L':'M')+a.x.toFixed(1)+' '+a.y.toFixed(1)).join(' '),area=line+' L 498 220 L 42 220 Z';host.innerHTML=`<svg viewBox="0 0 540 250" role="img" aria-label="Elevation profile"><defs><linearGradient id="rwElev" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="var(--rw-accent2)" stop-opacity=".45"/><stop offset="1" stop-color="var(--rw-accent2)" stop-opacity=".03"/></linearGradient></defs><path d="${area}" fill="url(#rwElev)"/><path d="${line}" fill="none" stroke="var(--rw-accent2)" stroke-width="4" stroke-linecap="round" stroke-linejoin="round"/>${pts.map(a=>`<circle cx="${a.x}" cy="${a.y}" r="5" fill="#fff" stroke="var(--rw-accent2)" stroke-width="3"/>`).join('')}<text x="42" y="238" fill="#8f98a5" font-size="10">${esc(shortName(v[0].name))}</text><text x="498" y="238" text-anchor="end" fill="#8f98a5" font-size="10">${esc(shortName(v[v.length-1].name))}</text><text x="42" y="24" fill="#fff" font-size="12" font-weight="800">High ${Math.round(max).toLocaleString()} m</text><text x="498" y="24" text-anchor="end" fill="#aab2bd" font-size="11">Low ${Math.round(min).toLocaleString()} m</text></svg>`;if(status)status.textContent=Math.round(max).toLocaleString()+' m high'}
function miniMap(day){const host=$(`[data-rw-mini-map="${day}"]`);if(!host)return;const p=state.route.find(x=>Number(x.day)===Number(day)&&x.geo)||state.route.find(x=>x.geo);if(!p){host.innerHTML='<div class="rw-v4-mini-fallback">Route point unavailable · use the route atlas above.</div>';return}host.innerHTML=`<div class="rw-v4-mini-pin"><span>⌖</span><div><b>${esc(p.name)}</b><small>${p.geo.lat.toFixed(3)}, ${p.geo.lng.toFixed(3)}</small></div><button data-rw-open-mini>Open map ↗</button></div>`;$('[data-rw-open-mini]',host)?.addEventListener('click',()=>window.open('https://www.openstreetmap.org/?mlat='+p.geo.lat+'&mlon='+p.geo.lng+'#map=12/'+p.geo.lat+'/'+p.geo.lng,'_blank','noopener'))}
function paintVisibleMiniMaps(){$$('.rw-v4-mini-map:not([hidden])').forEach(x=>miniMap(Number(x.dataset.rwMiniMap)))}
function wireCinematic(mount,days,ctx){mount.addEventListener('click',e=>{const a=e.target.closest('[data-rw-v4-action]');if(a){const k=a.dataset.rwV4Action;if(k==='classic')return setMode('classic');if(k==='map')return $('#rwV4Atlas')?.scrollIntoView({behavior:'smooth',block:'center'});if(k==='pdf')return openPremiumPdfFromCinematic(ctx);if(k==='share')return shareCinematic(ctx)}const j=e.target.closest('[data-rw-jump]');if(j)return jumpDay(+j.dataset.rwJump);const s=e.target.closest('[data-rw-step]');if(s)return jumpDay(state.carouselIndex+(+s.dataset.rwStep));const det=e.target.closest('[data-rw-details]');if(det){const x=$(`[data-rw-full="${det.dataset.rwDetails}"]`,mount);if(x){x.hidden=!x.hidden;det.textContent=x.hidden?'Full details':'Hide details'}return}const dm=e.target.closest('[data-rw-day-map]');if(dm){const h=$(`[data-rw-mini-map="${dm.dataset.rwDayMap}"]`,mount);if(h){h.hidden=!h.hidden;if(!h.hidden)miniMap(Number(dm.dataset.rwDayMap))}}});const car=$('#rwV4Carousel',mount);let t;car?.addEventListener('scroll',()=>{clearTimeout(t);t=setTimeout(()=>updateDayIndex(days.length),70)},{passive:true});car?.addEventListener('keydown',e=>{if(e.key==='ArrowRight')jumpDay(state.carouselIndex+1);if(e.key==='ArrowLeft')jumpDay(state.carouselIndex-1)});if(car)car.tabIndex=0}
function jumpDay(i){const cards=$$('.rw-v4-day'),n=clamp(i,0,cards.length-1);cards[n]?.scrollIntoView({behavior:'smooth',block:'nearest',inline:'center'});state.carouselIndex=n;paintDayNav(cards.length)}
function updateDayIndex(total){const car=$('#rwV4Carousel'),cards=$$('.rw-v4-day',car||document);if(!car||!cards.length)return;const c=car.scrollLeft+car.clientWidth/2;let best=0,dist=Infinity;cards.forEach((x,i)=>{const d=Math.abs(x.offsetLeft+x.offsetWidth/2-c);if(d<dist){dist=d;best=i}});state.carouselIndex=best;paintDayNav(total)}
function paintDayNav(total){const c=$('#rwV4Current');if(c)c.textContent=state.carouselIndex+1;$$('[data-rw-jump]').forEach((x,i)=>x.classList.toggle('on',i===state.carouselIndex))}
function shareCinematic(ctx){const text='My '+ctx.days+'-day '+ctx.destination+' journey — made by RoamWise';if(navigator.share)navigator.share({title:ctx.destination+' · RoamWise',text,url:location.href}).catch(()=>{});else navigator.clipboard?.writeText(location.href).then(()=>toast('Link copied'))}
function openPremiumPdfFromCinematic(ctx){try{const b=$$('#results button,#results a').find(x=>/premium\s*pdf|pdf itinerary/i.test(clean(x.textContent))||/openPdfFlow/.test(x.getAttribute('onclick')||''));if(b){b.click();return}}catch(_){ }try{if(typeof window.openPdfFlow==='function')window.openPdfFlow('',ctx.destination,ctx.days,$('#month')?.value||'')}catch(_){toast('Premium PDF is available from the Classic itinerary actions.')}}
function installPdfPackHook(){if(state.pdfHooked)return;const original=window.openPdfFlow;if(typeof original!=='function'){setTimeout(installPdfPackHook,600);return}state.pdfHooked=true;window.__rwOriginalOpenPdfFlow=original;window.openPdfFlow=function(){const out=original.apply(this,arguments);setTimeout(decoratePdfFlow,30);return out};window.rwUnlockItineraryPack=function(){sessionStorage.setItem('rw_pdf_pack_unlocked','1');decoratePdfFlow(true)};window.rwDownloadClassicPdf=function(){if(!pdfPackAllowed())return;if(typeof window.genPdf==='function')window.genPdf(false)};window.rwDownloadCinematicPdf=downloadCinematicPdf;window.rwDownloadPresetPdf=downloadPresetPdf}
function pdfPackAllowed(){return isPro()||sessionStorage.getItem('rw_pdf_pack_unlocked')==='1'}
async function matchingPreset(ctx){try{if(!window.RW_PRESETS?.find)return null;return await window.RW_PRESETS.find({destination:ctx.destination,duration:ctx.days,month:$('#month')?.value||'',style:$('#style')?.value||'',crowd:$('#crowd')?.value||'',budgetExact:$('#budgetExact')?.value||'',tags:ctx.tags||[]})}catch(_){return null}}
async function decoratePdfFlow(unlockedNow){const body=$('#pdfBody');if(!body)return;const ctx=formContext(),hit=await matchingPreset(ctx),allowed=pdfPackAllowed();$('#rwPdfPack')?.remove();const box=document.createElement('div');box.id='rwPdfPack';box.className='rw-pdf-pack';box.innerHTML=`<div class="rw-pdf-pack-head"><span>✦</span><div><b>Premium itinerary pack</b><small>One existing ₹10 unlock · no extra charge for Cinematic</small></div></div><div class="rw-pdf-pack-includes"><span>Classic PDF</span><span>Cinematic PDF</span>${hit?'<span>Matching ready-made PDF</span>':''}</div>${allowed?`<div class="rw-pdf-pack-actions"><button onclick="rwDownloadClassicPdf()">↓ Classic PDF</button><button class="primary" onclick="rwDownloadCinematicPdf()">✦ Cinematic PDF</button>${hit?'<button onclick="rwDownloadPresetPdf()">↓ Ready-made PDF</button>':''}</div>`:'<div class="rw-pdf-lock-note">Pay the existing ₹10 once, then unlock every available PDF format for this itinerary.</div>'}`;body.prepend(box);window.__rwPresetPdfHit=hit;if(!allowed){const paid=$$('button',body).find(b=>/i.?ve paid.*generate full pdf/i.test(clean(b.textContent)));if(paid){paid.textContent='✅ I’ve paid ₹10 — Unlock itinerary pack';paid.setAttribute('onclick','track(\'pdf_paid\');rwUnlockItineraryPack()')}}if(unlockedNow)toast('Itinerary pack unlocked · choose any PDF format')}
function loadJsPdf(cb){if(window.jspdf)return cb();if(typeof window.loadJsPdf==='function'&&window.loadJsPdf!==loadJsPdf)return window.loadJsPdf(cb);const s=document.createElement('script');s.src='https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js';s.onload=cb;s.onerror=()=>toast('PDF engine needs internet');document.head.appendChild(s)}
function pdfText(v){return String(v||'').replace(/[\u2018\u2019]/g,"'").replace(/[\u201c\u201d]/g,'"').replace(/[\u2013\u2014]/g,'-').replace(/₹/g,'Rs ').replace(/[^\x20-\x7E\n]/g,'')}
function downloadCinematicPdf(){if(!pdfPackAllowed())return toast('Unlock the premium itinerary pack first.');const ctx=formContext(),days=extractDays(ctx);if(!days.length)return toast('Generate the itinerary first.');loadJsPdf(()=>{const J=window.jspdf?.jsPDF;if(!J)return;const pdf=new J({unit:'px',format:[600,800]});const W=600,H=800,accent=[196,48,43],gold=[232,186,108];function pageBg(){pdf.setFillColor(10,13,18);pdf.rect(0,0,W,H,'F')}function line(t,x,y,size=12,col=[225,230,236],max=510,bold=false){pdf.setFont('helvetica',bold?'bold':'normal');pdf.setFontSize(size);pdf.setTextColor(...col);const a=pdf.splitTextToSize(pdfText(t),max);pdf.text(a,x,y);return y+a.length*(size*1.25)}pageBg();pdf.setTextColor(...gold);pdf.setFont('helvetica','bold');pdf.setFontSize(11);pdf.text('ROAMWISE  /  CINEMATIC JOURNEY',44,70);pdf.setTextColor(255,255,255);pdf.setFontSize(36);const tt=pdf.splitTextToSize(pdfText(ctx.destination),500);pdf.text(tt,44,135);let y=135+tt.length*44;pdf.setFillColor(...accent);pdf.roundedRect(44,y+12,112,5,2,2,'F');y+=55;y=line(ctx.days+' days  /  '+ctx.month+'  /  '+ctx.style,44,y,15,[205,212,221],500);y=line('A visual itinerary built from your RoamWise plan.',44,y+28,15,[205,212,221],500);pdf.setDrawColor(52,58,67);pdf.line(44,650,556,650);const traveler=clean($('#pdfName')?.value||localStorage.getItem('rw_name')||'');line('Made by RoamWise'+(traveler?' for '+traveler:''),44,690,11,gold,500,true);days.forEach((d,i)=>{pdf.addPage([600,800]);pageBg();pdf.setFillColor(...accent);pdf.roundedRect(36,36,70,34,12,12,'F');pdf.setTextColor(255,255,255);pdf.setFont('helvetica','bold');pdf.setFontSize(12);pdf.text('DAY '+d.n,51,58);let yy=116;yy=line(d.title,36,yy,27,[255,255,255],520,true)+22;const sections=[['MORNING',d.morning],['AFTERNOON',d.afternoon],['EVENING',d.evening],['LOCAL NOTE',d.tip],['STAY',d.stay],['FOOD',d.food],['BUDGET',d.budget]].filter(x=>x[1]);sections.forEach(([lab,val])=>{if(yy>700)return;yy=line(lab,36,yy,9,gold,520,true)+8;yy=line(val,36,yy,13,[211,216,224],520)+22});pdf.setFontSize(9);pdf.setTextColor(112,120,132);pdf.text('RoamWise cinematic itinerary  -  verify live conditions before travel',36,760)});pdf.save('RoamWise-'+ctx.destination.replace(/[^a-z0-9]+/gi,'-')+'-Cinematic.pdf');toast('Cinematic PDF downloaded')})}
function downloadPresetPdf(){if(!pdfPackAllowed())return toast('Unlock the premium itinerary pack first.');const h=window.__rwPresetPdfHit;if(!h?.pdf)return toast('No matching ready-made PDF for these criteria.');const a=document.createElement('a');a.href=h.pdf;a.download='';a.target='_blank';a.rel='noopener';document.body.appendChild(a);a.click();a.remove();toast('Matching ready-made itinerary opened')}
function observeClassic(){state.observer?.disconnect();const root=$('#results')?.parentElement||document.body;state.observer=new MutationObserver(()=>{clearTimeout(observeClassic.t);observeClassic.t=setTimeout(()=>{ensureInlineButton();installPdfPackHook();const fp=fingerprint();if(state.mode==='cinematic'&&fp&&fp!==state.lastFingerprint){state.lastFingerprint=fp;renderCinematic()}},100)});state.observer.observe(root,{childList:true,subtree:true,characterData:true});ensureInlineButton()}
function init(){ensureStyles();installPdfPackHook();observeClassic();setTimeout(ensureInlineButton,500);setTimeout(ensureInlineButton,1800)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
window.RW_CINEMATIC={version:VERSION,setMode,render:()=>setMode('cinematic'),classic:()=>setMode('classic'),downloadCinematicPdf};
})();
