# RoamWise Pro — Project State (read this first, every session)

**Purpose of this file:** Claude's sandbox can reset mid-project (it happened once already — lost the Android SDK, keystore, and agent folder). This file is the single source of truth so a fresh session can pick up correctly without re-deriving everything from a long chat transcript. Update it whenever something structural changes.

## 🚨 Critical — read before touching the Android build
- **The signing keystore (`rw.keystore`) is the single most important file in this project.** Location: `/home/claude/rw2/apk/build/rw.keystore` (both the old manual toolchain AND the new Gradle project reference this same file — don't duplicate it).
- **Mohit: confirm you have `RoamWise-KEYSTORE-BACKUP.zip` saved somewhere durable.** Losing it after Play Store publishing permanently breaks the ability to ship updates.

## ⚡ Build system: NOW GRADLE-BASED (as of v10.2) — the old manual toolchain is deprecated
The project moved from a manual `aapt2`+`javac`+`d8` pipeline to a **real Gradle project** at `/home/claude/rw2/gradleapp/`, specifically to support proper Maven dependency resolution (needed for the `android-youtube-player` native library — see below). **Use Gradle for all future Android builds; the old `/home/claude/rw2/apk/proj` manual pipeline is legacy and no longer the source of truth for MainActivity.java.**

**Toolchain as of v10.6 (SDK-36 rebuild):** Gradle **8.11.1** (`/home/claude/rw2/gradle-8.11.1/bin`) + AGP **8.10.1** + `platforms;android-36` + `build-tools;36.0.0` (installed in the same SDK dir). compileSdk 36 / targetSdk 36 / minSdk 24. **targetSdk 35+ enforces edge-to-edge on Android 15/16** — MainActivity handles this with a WindowInsets listener padding the root layout (dark #0A0A0C background behind the bars), zeroed during HTML5-fullscreen video and restored via `requestApplyInsets()` on exit. Do not remove that listener or the header will render under the status bar on modern devices.

**R8 minification ON as of v10.7 (versionCode 20):** `minifyEnabled true` + `shrinkResources true` cut the app from ~2.7MB to ~900KB. Keep rules live in `app/proguard-rules.pro` — the critical one preserves every `@JavascriptInterface` method unrenamed (verified in mapping.txt: saveCard/openExternal/playPromoVideo/openLastSaved all map to themselves). **Never delete proguard-rules.pro or the entire JS bridge silently breaks.** The deobfuscation map auto-embeds in the AAB (`BUNDLE-METADATA/com.android.tools.build.obfuscation/proguard.map`), which is what clears Play Console's "no deobfuscation file" warning — no manual mapping upload needed. Standalone mapping.txt per release lands at `app/build/outputs/mapping/release/mapping.txt`.

**v50 — view isolation, single film player, dead-code purge:** (1) **Each icon now opens only its own thing.** `v-extras` was doing triple duty, so the Store tab showed ratings + film + creator. Split into `v-store` (store), `v-film` (billboard + creator + The Film), `v-extras` (traveler wall), `v-copilot` (hero). Verified per-view: store->[store], film->[promoTop,creator], extras->[ratings], copilot->[copilotHero]. `VIEW_OF` remapped to match; `RW_TABS` gained film + ratings entries and Copilot now opens its own view instead of bouncing to Home. (2) **Plan lives in two places** — `#app`/`#results` tagged `v-plan v-home`, so people can scroll the home page OR tap the Plan icon. (3) **ONE film player.** There were TWO competing implementations (billboard `playPromo` built `#promoVideo`; the film section built `#filmInline`), which is why behaviour differed by where you tapped. Consolidated into `filmPlayerHTML()` + `filmAttachDiagnostics()`, used by both. Diagnostics now print the REAL `MediaError` code (aborted/network/decode/format) plus the actual URL, and offer "Open in device player" before YouTube — the old vague "watch on YouTube" card hid the cause for several releases. Also listens on the `<source>` element, since source failures do not reliably fire `error` on the video. Verified live: promo.mp4 is HTTP 200, H.264/AAC, fast-start, 206 range — headless Chromium reports format-unsupported only because it lacks the proprietary H.264 codec. (4) **Marketing hero hidden in-app** (`.v-web-only` + `body.in-app`) — it is a landing-page pitch, pure clutter for someone who already installed. (5) **Carousel photos**: `findURL` now tries name -> name+country -> "name city" -> "country landmark" before giving up, so small towns stop rendering as flat gradients. Verified 6/6 on both rows. (6) **Dead code purged** (~9KB): `_playPromo_ABANDONED_inlineAttempt`, `swapPromoDomain`, `_ytApiLoading`, `toggleSound`, and the whole unreachable SHINOBI PHONK generator (sndStart/sndStop/phKick/ph808/phHat/phCow/phSchedule/PH_BPM/COW) after confirming no external references. **BUG I CAUSED AND CAUGHT: the phonk block removal also swallowed the adjacent `var ADS=[...]` definition, so `ADS is not defined` threw at load — halting the script BEFORE `var RW_TABS = {...}` executed, which left the tab bar empty while hoisted functions still existed.** Recovered ADS from the v49 build assets and added a symbol-diff audit (old vs new top-level `function`/`var` names) to prove only the intended 14 symbols were removed. **LESSON: when deleting a block by scanning to the next `function`, anything between the block and that function goes too — diff the symbol table afterwards.** Note: the recurring `'Y'` page errors seen in headless tests come from Firebase's minified bundle failing to connect in the sandbox, not from app code.

**v49 — MODULAR SPLIT + navigation overhaul:** **index.html 623KB -> 64KB.** All CSS extracted to `web/app.css` (68KB, 716 rules) and all inline JS to `web/app.js` (495KB), linked with `<script src="app.js" defer>`. **BUILD CHANGE (critical): `PLAY_MODE` now lives in app.js, so the AAB step seds app.js NOT index.html, and all THREE files must be copied into `gradleapp/app/src/main/assets/`.** SW precache updated to `/index.html`, `/app.css`, `/app.js`. Honest note recorded: splitting does not by itself "scale to millions" (that is CDN + caching, which GitHub Pages already provides) — what it buys is maintainability and separate cache lifetimes. **Customisable bottom nav**: `RW_TABS` registry (home/plan/copilot/explore/map/store/trips/group/pro/settings/more) + `rwTabIds`/`renderTabbar`/`rwTabGo`/`rwTabToggle`; user picks 3-5 in Settings via `rwTabPickerHTML()`; default is home/plan/copilot/store/more. Adding a destination = one RW_TABS line. **Home decluttered**: ratings/store/creator retagged `v-extras` (new view) and reachable via the Store tab + drawer link. **Desktop no longer dumps everything in one scroll** — `applyShell()` now always enables the tabbed view system (was mobile-only); verified 2 of 16 sections visible at 1440px. **Film plays inline** — `renderPromo` now renders a real `<video>` with the self-hosted promo.mp4 (verified live: HTTP 200, H.264/AAC, fast-start moov, 206 range support) and demotes YouTube to a quiet secondary link. Note: headless Chromium reports MEDIA_ERR_SRC_NOT_SUPPORTED for H.264 because it lacks the proprietary codec — NOT a real failure. **Sound icon removed.** **Copilot hero enlarged** (132px input, 4 rows, titled "Ailon Tusk") as the main attraction. **Real attractions layer**: `osmAttractions()` queries OpenStreetMap Overpass (free, keyless, worldwide) for viewpoints/waterfalls/ruins/temples/peaks/hot springs within 12km, de-duped, capped at 30, cached 30 days per 0.01-degree cell — surfaces the small stuff big apps skip; wired into Copilot answers and the live-map tap panel with OSM attribution. **THREE bugs found by testing:** (a) my `s.find('</nav>')` searched from index 0 and matched an EARLIER nav, so the replacement injected an empty tabbar BEFORE `<!DOCTYPE html>` and left the hardcoded one intact — always search for the closing tag FROM the opening index; (b) duplicate `tb-*` ids from the old hardcoded bar broke tab-state matching; (c) `renderTabbar()` called inline ran before the `var RW_TABS = {...}` assignment (function declarations hoist, var assignments do not) and silently produced an empty bar inside its try/catch — moved to DOMContentLoaded, which fires after all deferred script.

**v48 — Ailon Tusk gets smarter + a personality:** THREE things Mohit asked for, built as the honest versions. (1) **Vibe/intent inference** in `cpParseRegex`: reads mood + constraints behind the words — relax/adventure/party/romantic/family/spiritual/nature/food/budget/luxury/solo/workation, plus proximity ("near Delhi"), fuzzy durations ("long weekend"->3, "week"->7), and season (summer/winter/monsoon). Turns "somewhere chill near Delhi for a long weekend, not too pricey" into structured intent. **Critical fix:** mood words (romantic, solo, chill...) were being captured AS destinations — added them to both the stopword filter and the preposition-capture VERBS guard; verified Manali/Goa/Rishikesh now resolve with vibes captured separately. (2) **Self-improving profile** (`rwLearnIntent`/`rwUserProfile`): every parse feeds an on-device profile (top vibe, avg budget, typical days); Copilot reads it back to personalise once count>=3. Honest "learns from user data" — stays on-device, nothing leaves unless user opts into aggregate sharing. (3) **Personality + voice notes**: `TUSK_QUIPS` = ORIGINAL witty Hinglish/filmy lines by category (NOT copyrighted Bollywood dialogue/lyrics — that would get the app pulled), spoken via `tuskSpeak` (native `RW.speak()` TextToSpeech bridge in-app, Web Speech on web). `tuskVoiceNoteHTML` renders a play-button voice-note bubble in destination answers. **Agent:** `agent-pkg/agent/tusk-refresh.js` — the honest "self-updating engine": re-fetches Wikivoyage guide digests for a seed list on a schedule, writes reviewable `tusk-knowledge.json`, grows coverage. Does NOT crawl Google or train a model (both impossible/disallowed — documented plainly in TUSK-AGENT-SETUP.md). Retries with backoff + 900ms spacing; verified 2/2 and 9-section extraction when not rate-limited. **Native TTS bridge** added to MainActivity (`RW.speak()`, @JavascriptInterface, R8-verified kept). **Stance held: no Google crawling, no untraceable/unhackable code (ships as readable JS in an APK — physics, not choice), no real-LLM-beating claim, no copyrighted audio.**

**v47 / admin v2.0 — group engine, secure chat, carousel photos FINALLY fixed, bigger cards:** (1) **Group Compromise Engine** (`grpCompromise`) — pure deterministic logic, no server/AI cost: each member sets interests/budget/diet, `grpScoreMember` blends interest overlap (0.5) + budget fit (0.35) + diet comfort (0.15), and fairness = avg*0.6 + min*0.4 (the min term stops "great for 4, awful for 1" winning). Shows each person's satisfaction bar so the pick is provably fair. Group tile in Quick start. (2) **Secure Trip Chat** (`tripChatOpen`/`tripChatForTrip`/`tripChatById`) — private room per trip in Firestore `tripchats/{roomId}` + `msgs` subcollection; "secure" is specific: member-array-enforced in RULES (not just UI), messages signed with sender uid, unguessable room id, NOT E2E and the UI says so. Deterministic room id `trip_<slug>_<idsuffix>`. Live `onSnapshot`. Rules added: `tripchats` block (member-in-array read/write, self-join only, msg create requires uid match + membership). (3) **CAROUSEL PHOTOS — root cause was RATE LIMITING**: at page load a burst of `wikiAny` calls hit Wikipedia's REST API limit and returned null, which got cached as `''` PERMANENTLY, so cards never painted. Chain of earlier wrong turns: `wikiThumb` rewrote the REST URL to `/640px-` (404s), and Wikimedia hotlink-protects direct browser image loads (need the weserv proxy WITH full `encodeURIComponent` of the whole source URL). Final fix: `wikiThumb` returns the REST URL as-is, `wikiAny` tries REST-summary first (proxy-safe), painter routes through `images.weserv.nl/?url=<ENCODED>`, `findURL` RETRIES with backoff instead of caching empty, and only successes are cached. Verified 6/6 cards paint on both rows. (4) Cards enlarged 128px->168px, aspect 3/4, bigger labels, hover-lift, shadow. **Lesson: a burst of API calls at load can rate-limit and cache poison — retry with backoff, cache only successes.**

**v50 — view isolation, single film player, dead-code purge:** (1) **Each icon now opens only its own thing.** `v-extras` was doing triple duty, so the Store tab showed ratings + film + creator. Split into `v-store` (store), `v-film` (billboard + creator + The Film), `v-extras` (traveler wall), `v-copilot` (hero). Verified per-view: store->[store], film->[promoTop,creator], extras->[ratings], copilot->[copilotHero]. `VIEW_OF` remapped to match; `RW_TABS` gained film + ratings entries and Copilot now opens its own view instead of bouncing to Home. (2) **Plan lives in two places** — `#app`/`#results` tagged `v-plan v-home`, so people can scroll the home page OR tap the Plan icon. (3) **ONE film player.** There were TWO competing implementations (billboard `playPromo` built `#promoVideo`; the film section built `#filmInline`), which is why behaviour differed by where you tapped. Consolidated into `filmPlayerHTML()` + `filmAttachDiagnostics()`, used by both. Diagnostics now print the REAL `MediaError` code (aborted/network/decode/format) plus the actual URL, and offer "Open in device player" before YouTube — the old vague "watch on YouTube" card hid the cause for several releases. Also listens on the `<source>` element, since source failures do not reliably fire `error` on the video. Verified live: promo.mp4 is HTTP 200, H.264/AAC, fast-start, 206 range — headless Chromium reports format-unsupported only because it lacks the proprietary H.264 codec. (4) **Marketing hero hidden in-app** (`.v-web-only` + `body.in-app`) — it is a landing-page pitch, pure clutter for someone who already installed. (5) **Carousel photos**: `findURL` now tries name -> name+country -> "name city" -> "country landmark" before giving up, so small towns stop rendering as flat gradients. Verified 6/6 on both rows. (6) **Dead code purged** (~9KB): `_playPromo_ABANDONED_inlineAttempt`, `swapPromoDomain`, `_ytApiLoading`, `toggleSound`, and the whole unreachable SHINOBI PHONK generator (sndStart/sndStop/phKick/ph808/phHat/phCow/phSchedule/PH_BPM/COW) after confirming no external references. **BUG I CAUSED AND CAUGHT: the phonk block removal also swallowed the adjacent `var ADS=[...]` definition, so `ADS is not defined` threw at load — halting the script BEFORE `var RW_TABS = {...}` executed, which left the tab bar empty while hoisted functions still existed.** Recovered ADS from the v49 build assets and added a symbol-diff audit (old vs new top-level `function`/`var` names) to prove only the intended 14 symbols were removed. **LESSON: when deleting a block by scanning to the next `function`, anything between the block and that function goes too — diff the symbol table afterwards.** Note: the recurring `'Y'` page errors seen in headless tests come from Firebase's minified bundle failing to connect in the sandbox, not from app code.

**v49 — MODULAR SPLIT + navigation overhaul:** **index.html 623KB -> 64KB.** All CSS extracted to `web/app.css` (68KB, 716 rules) and all inline JS to `web/app.js` (495KB), linked with `<script src="app.js" defer>`. **BUILD CHANGE (critical): `PLAY_MODE` now lives in app.js, so the AAB step seds app.js NOT index.html, and all THREE files must be copied into `gradleapp/app/src/main/assets/`.** SW precache updated to `/index.html`, `/app.css`, `/app.js`. Honest note recorded: splitting does not by itself "scale to millions" (that is CDN + caching, which GitHub Pages already provides) — what it buys is maintainability and separate cache lifetimes. **Customisable bottom nav**: `RW_TABS` registry (home/plan/copilot/explore/map/store/trips/group/pro/settings/more) + `rwTabIds`/`renderTabbar`/`rwTabGo`/`rwTabToggle`; user picks 3-5 in Settings via `rwTabPickerHTML()`; default is home/plan/copilot/store/more. Adding a destination = one RW_TABS line. **Home decluttered**: ratings/store/creator retagged `v-extras` (new view) and reachable via the Store tab + drawer link. **Desktop no longer dumps everything in one scroll** — `applyShell()` now always enables the tabbed view system (was mobile-only); verified 2 of 16 sections visible at 1440px. **Film plays inline** — `renderPromo` now renders a real `<video>` with the self-hosted promo.mp4 (verified live: HTTP 200, H.264/AAC, fast-start moov, 206 range support) and demotes YouTube to a quiet secondary link. Note: headless Chromium reports MEDIA_ERR_SRC_NOT_SUPPORTED for H.264 because it lacks the proprietary codec — NOT a real failure. **Sound icon removed.** **Copilot hero enlarged** (132px input, 4 rows, titled "Ailon Tusk") as the main attraction. **Real attractions layer**: `osmAttractions()` queries OpenStreetMap Overpass (free, keyless, worldwide) for viewpoints/waterfalls/ruins/temples/peaks/hot springs within 12km, de-duped, capped at 30, cached 30 days per 0.01-degree cell — surfaces the small stuff big apps skip; wired into Copilot answers and the live-map tap panel with OSM attribution. **THREE bugs found by testing:** (a) my `s.find('</nav>')` searched from index 0 and matched an EARLIER nav, so the replacement injected an empty tabbar BEFORE `<!DOCTYPE html>` and left the hardcoded one intact — always search for the closing tag FROM the opening index; (b) duplicate `tb-*` ids from the old hardcoded bar broke tab-state matching; (c) `renderTabbar()` called inline ran before the `var RW_TABS = {...}` assignment (function declarations hoist, var assignments do not) and silently produced an empty bar inside its try/catch — moved to DOMContentLoaded, which fires after all deferred script.

**v48 — Ailon Tusk gets smarter + a personality:** THREE things Mohit asked for, built as the honest versions. (1) **Vibe/intent inference** in `cpParseRegex`: reads mood + constraints behind the words — relax/adventure/party/romantic/family/spiritual/nature/food/budget/luxury/solo/workation, plus proximity ("near Delhi"), fuzzy durations ("long weekend"->3, "week"->7), and season (summer/winter/monsoon). Turns "somewhere chill near Delhi for a long weekend, not too pricey" into structured intent. **Critical fix:** mood words (romantic, solo, chill...) were being captured AS destinations — added them to both the stopword filter and the preposition-capture VERBS guard; verified Manali/Goa/Rishikesh now resolve with vibes captured separately. (2) **Self-improving profile** (`rwLearnIntent`/`rwUserProfile`): every parse feeds an on-device profile (top vibe, avg budget, typical days); Copilot reads it back to personalise once count>=3. Honest "learns from user data" — stays on-device, nothing leaves unless user opts into aggregate sharing. (3) **Personality + voice notes**: `TUSK_QUIPS` = ORIGINAL witty Hinglish/filmy lines by category (NOT copyrighted Bollywood dialogue/lyrics — that would get the app pulled), spoken via `tuskSpeak` (native `RW.speak()` TextToSpeech bridge in-app, Web Speech on web). `tuskVoiceNoteHTML` renders a play-button voice-note bubble in destination answers. **Agent:** `agent-pkg/agent/tusk-refresh.js` — the honest "self-updating engine": re-fetches Wikivoyage guide digests for a seed list on a schedule, writes reviewable `tusk-knowledge.json`, grows coverage. Does NOT crawl Google or train a model (both impossible/disallowed — documented plainly in TUSK-AGENT-SETUP.md). Retries with backoff + 900ms spacing; verified 2/2 and 9-section extraction when not rate-limited. **Native TTS bridge** added to MainActivity (`RW.speak()`, @JavascriptInterface, R8-verified kept). **Stance held: no Google crawling, no untraceable/unhackable code (ships as readable JS in an APK — physics, not choice), no real-LLM-beating claim, no copyrighted audio.**

**v47 / admin v2.0 — group engine, secure chat, carousel photos FIXED, bigger cards:** (1) **Group Compromise Engine** (`grpCompromise`) — pure deterministic logic, no server/AI cost: members set interests/budget/diet; `grpScoreMember` scores each destination (interest overlap 0.5 + budget fit 0.35 + diet comfort 0.15); fairness = avg*0.6 + min*0.4 (the min term stops "great for 4, awful for 1"). Shows a per-person satisfaction bar so the pick is provably fair. `openGroupPlanner`/grpRender/grpAdd, Group tile in Quick start. Verified: 3-member group -> Hoi An fairness 61, everyone scored. (2) **Secure Trip Chat** (`tripChatOpen`/`tripChatForTrip`/`tripChatById`) — private room per trip (Firestore `tripchats/{roomId}` + msgs subcollection). "Secure" = member-array enforced IN RULES not just UI, messages signed with sender uid, unguessable room id, NOT E2E (UI says so honestly). Deterministic room id `trip_<slug>_<idsuffix>`; live `onSnapshot`. Chat button on saved trips. (3) **CAROUSEL PHOTOS FIXED** — the long-standing bug: `wikiThumb` rewrote the REST summary URL to `/640px-` which 404s, and `findURL` cached the null from Wikipedia's REST **rate-limiting the page-load burst** as a permanent empty string. Fixes: return the REST thumbnail URL as-is; try REST-summary before pageimages; retry wikiAny 2x with backoff; cache ONLY successes; route through weserv proxy with FULL `encodeURIComponent`; space the queue 350ms. Verified 6/6 cards paint on both rows from a cleared cache. (4) Cards enlarged 128->168px, aspect 3/4, bigger labels, hover lift, shadow. **Firestore rules: added `tripchats` block (member-array read/write, msg text<=1000, sender-signed); admin console FULL_RULES re-synced.** **Lesson: a null cached as a permanent negative is a trap when the null came from rate-limiting, not absence — retry before caching a miss.**

**v46 — Ailon Tusk knowledge layer (renamed from Delta 1.1):** "Crawl Google in the background" is not possible from a static app (Google blocks cross-origin browser calls, its terms forbid scraping, and the official Search API bills per query) — **Wikivoyage is the honest substitute**: real community travel guides for tens of thousands of places, `origin=*` CORS, no key, no cost, CC BY-SA so reuse with attribution is permitted. `wvGuide()` fetches intro extract + section index, `wvSection()` pulls and de-wikifies a specific section, `wvPickSections()` routes the question to the right one via `WV_SECTION_MAP` (eat/food -> Eat+Drink, how to reach -> Get in, safety -> Stay safe, etc). Both Copilot paths use it: keyless answers come from curated DB **then** the live guide; with a BYOK key the guide extract is injected as grounding with "prefer this over your own recollection", which is what stops confident-but-wrong answers about small towns. Everything cached 30 days in `rw_wv_*`/`rw_wvs_*` with attribution + source link always shown. **Learning is real but honestly named**: `rwLearn()` counts places actually engaged with (`rw_learn`), `rwTopInterests()` exposes them — no model weights are trained in a phone browser, and the UI never claims otherwise. **Two parser bugs found by testing:** "what **to eat** in Manali" captured "eat" as the destination because `/to\s+(\w+)/` matched before "in Manali" — now every preposition match is collected, verbs/fillers rejected, capitalised candidates preferred; and `==Get in==` headers survived wikitext cleanup. Verified across six question forms (eat/go/see/stay/bare-name/budget) — all resolve correctly.

**v45 — three "next-gen" features, built at zero cost:** (1) **Shadow Budget** (`shadowBudget`/`shadowBudgetHTML`) — the flagship differentiator: expands the DB's weekly `cost` tiers + `brk` breakdown into a daily cash-flow plus the costs nobody quotes: airport transfers (3.2x local-transit day rate), tourist SIM ($8, skipped domestically), ATM/card FX spread (2.5%, skipped domestically), region-based tipping (`TIP_BY_REGION`: Japan 0%, US 18%, SE/South Asia 5%), and a 10% buffer; ends with a cash-vs-card split. Live USD->INR from Frankfurter cached in `window._rwFxINR` (no more hardcoded 88). **The curated DB has only 15 countries and NO India**, so domestic trips got nothing — added `RW_COST_HINTS` (19 countries + regional default) and `costEntryForPlace(geo)` so any geocoded place gets a band. Everything is labelled "estimates, not live quotes". (2) **Rain contingency** — `rainSwapHTML` reads the 7-day forecast already fetched for a saved trip, classifies each planned day indoor/outdoor via `OUTDOOR_RE`/`INDOOR_RE` keyword scoring, and offers a concrete Day-N <-> Day-M swap; `rainSwapApply` rewrites the saved trip and renumbers days, only on tap — never silently. (3) **Camera -> itinerary** (`scanImageOpen`/`scanImageRun`) — camera button inside the hero box; sends the image to gemini-2.5-flash inline_data on the USER'S key (vision-capable free tier), parses strict JSON, lists extracted places each with a Plan button; with no key it says what it needs instead of faking. **`RW_PLACE_OVERRIDES`** (40 famous Indian destinations with exact lat/lon) now short-circuits `rwResolvePlace` — the geocoder ranks by population, so "Manali" resolved to a CHENNAI SUBURB and "Bir" to Ukraine; overrides fix the exact queries an India-first app must get right, checked before the network call so they also work offline. NOT built (honest scope): swipeable video feed (no rights-cleared video source at zero cost; YouTube path already ruled out) and the group compromise engine (pure logic, buildable next).

**Admin v1.9 — index error removed at the source + revenue counted correctly:** The honest error handler immediately exposed the real fault: `stats` used `.orderBy(FieldPath.documentId(),'desc').limit(7)`, and a DESCENDING order on `__name__` needs a composite index Firestore does not auto-create (`failed-precondition`). Rather than make Mohit create an index, the query is gone: day docs are named `YYYY-MM-DD`, so `loadStats()` computes the last 7 IDs and does 7 direct `.doc(id).get()` reads — no index, no ordering, works in any project forever. **RULE: never use orderBy on documentId in this console; fetch day docs by computed ID.** Revenue fix: the `sRev` card summed ONLY `payments` (gateway, paise) so three real ₹100 UPI payments showed ₹0. Both `sRev` and `renderKpis` now total approved `claims` (rupees) + gateway payments, and the card label breaks it down ("UPI ₹300 · gateway ₹0"). Verified: 3 approved ₹100 claims render ₹300 and pending claims are excluded.

**v44 / admin v1.8 — "stats broken" was a LYING ERROR MESSAGE:** With Doctor all-green (including the admin-only config write, which proves admin status), the Dashboard still showed a rules error. Cause was in the console, not Firebase: (a) the funnel placeholder literally read "Loading stats… (needs the stats rules block published)", so a normal loading/empty state looked like a permissions failure, and (b) the `onSnapshot` error handler hardcoded "Firestore rules are blocking stats" for EVERY error class. Both fixed — placeholder is neutral, the handler now prints the real `code + message` and only offers the rules fix when `code==='permission-denied'`, and `renderFunnel()` returns an honest empty state ("No stats recorded yet… permissions are fine, nothing counted yet; docs appear as stats/YYYY-MM-DD") instead of blank bars. Also fixed a silent-failure in the app: `track()` wrapped `db...set()` in try/catch, but that promise rejects ASYNCHRONOUSLY so blocked writes vanished with no trace — it now records the last failure to `rw_track_err`. Doctor gained a **stats write** probe (the exact operation track() performs). **Lesson: an error message that names one cause for every failure sends the user to fix things that were never broken — report the actual error code.**

**Admin v1.6 — Rules Doctor + rules changelog:** New **Doctor tab** runs live read/write probes against admins, users, users/{uid}/devices (Sync Circle), pulse r+w (Travel Pulse), config r+w, crm, stats, secrets and claims, printing PASS/FAIL with the exact Firestore error code, and checks `admins/{uid}.ok===true` FIRST because a missing admin doc breaks every admin-gated feature even with perfect rules. It also prints the live `pulse` document count, because **Travel Pulse is invisible by design until a destination+month has 2+ planners** — writes succeeding with a low count means "waiting for traffic", not "broken". Shipped `FIRESTORE-RULES-CHANGES.md` (in the site zip + outputs): a line-numbered map of all 16 rule blocks with the three post-launch additions called out — `crm` L131-135 (v34), `config` L124-130 (v35), `secrets` L116-122 (v42) — plus the "search your published rules for the word secrets" staleness test and an explanation of why the GitHub agent keeps working while the console fails (service account bypasses rules).

**v43 — key persistence fixed at the DEFAULT, not just the feature:** v42's encrypted backup worked but was opt-in and buried behind Settings > Advanced, so a user who simply signed out still lost keys that were never backed up. Root fix: `wipeSession()` **no longer deletes `rwKey_*` by default** — these are the user's own third-party credentials, not an account entitlement (Pro flags are still cleared). Shared-device users can opt back in via `rw_wipe_keys` toggle in the Settings panel. Added `rwAutoBackup()` — a silent encrypted push fired after every successful key save when a remembered passphrase exists, so "save once" is literally once. `rwOfferBackup()` prompts for a passphrase at the moment the first key is saved while signed in (once; `rw_sec_declined` remembers a no). Panel now shows explicit status ("Backup is on/off"). **Lesson: when a user reports a feature "not working", check whether the DEFAULT path reaches them at all before debugging the feature.**

**v42 — end-to-end encrypted key sync:** Keys vanished on sign-out because `wipeSession()` deliberately clears every `rwKey_*` (shared-device safety) — that behaviour stays, but is now recoverable. New module: PBKDF2-SHA256 **210k iterations** -> AES-GCM-256, encrypting the key bundle IN THE BROWSER before it ever leaves; Firestore doc `secrets/{uid}` holds `{v,salt,iv,blob}` ciphertext only. Rules: `match /secrets/{uid} { allow read, write, delete: if isSelf(uid); }` — **owner-only, deliberately NOT admin-readable**. **Design note for future sessions: the passphrase is required and must never be replaced with something auto-derived (UID, email, constant) — anything the app can derive, a leaked backup or bad rules deploy can also derive, which would be encryption theatre. Forgetting the passphrase = unrecoverable, and that is the proof the server cannot read it.** UI: `secPanelHTML()` renders above the provider rows in Settings (Save / Restore / Delete + "remember on this device"); `rw_sec_pass` in localStorage enables silent auto-restore on the auth-state change at sign-in. Verified: round-trip OK, wrong passphrase rejected, zero plaintext in the payload, ~200ms derivation on mobile-class hardware. Admin console FULL_RULES re-synced (the standing rule after any rules change).

**v41 — provider fixes + native voice:** THREE root causes found. (1) **NVIDIA NIM is CORS-blocked** — its preflight returns 200 with NO `access-control-allow-origin`, so a browser/WebView app can never call it. Removed and replaced with **GitHub Models** (`models.inference.ai.azure.com`, OpenAI-compatible, CORS-allowed, free with a GitHub account, `ghp_`/`github_pat_` keys). Verified from a real browser page: cerebras/groq/github all return HTTP 401 with a bad key = reachable; NVIDIA was the only true blocker. (2) **Cerebras "save & test" failed because of key sniffing**: `keyProvider()` ended with a catch-all `/^[A-Za-z0-9]{32,}$/ => mistral`, so an unprefixed key got tested against the WRONG API. The catch-all is deleted — unknown format now returns null and the caller keeps the provider the user selected. Also error extraction now reads flat `{message}` bodies (Cerebras/others) instead of only `{error:{message}}`, so failures show the real reason. (3) **New providers were missing from Settings** because the key rows were hand-written HTML; they are now GENERATED from `PROV_META` via `renderKeyBoxes()` (called at openSettings), with a green "no card" badge — adding a provider to PROV_META is now the only step. **Voice:** Android WebView does NOT implement the Web Speech API, so the mic was dead in the APK while fine in Chrome. Added `RECORD_AUDIO` + `<queries>` for RecognitionService in the manifest and a native `SpeechRecognizer` bridge — `RW.startVoice()` (@JavascriptInterface, R8-verified unrenamed) with runtime permission handling, calling back into `rwVoiceResult(text)` / `rwVoiceState(state,msg)`. JS prefers the bridge when `window.RW` exists and falls back to Web Speech on the web.

**v40 — CRASH FIX + inline map + card-free AI providers:** **ROOT CAUSE of the "My Trips hangs, cannot scroll, cannot go back" bug: the `.sheet` / `.sheet-head` CSS classes NEVER EXISTED.** I invented them for the Trips/Copilot/Map overlays; unstyled, the container had no max-height and no overflow, so long lists ran past the viewport while `body{overflow:hidden}` blocked scrolling and pushed the close button off-screen. Fixed by adding real `.sheet` CSS (flex column, max-height 88dvh, sticky header, inner `overflow-y:auto`, `min-height:0` so flex children can shrink). **LESSON: never reference a CSS class without grepping that it exists.** Also added `rwOverlayOpen/rwOverlayClose` + `popstate` handling — overlays now push a history entry so Android hardware BACK closes them instead of exiting the app (MainActivity already routes back to `web.goBack()`). Map moved from overlay to **inline `#mapSection`** injected after `#copilotHero` (scroll/back keep working around it). **AI providers reworked after the user hit billing walls:** added Cerebras (`api.cerebras.ai/v1`, ~1M tokens/day) and NVIDIA NIM (`integrate.api.nvidia.com/v1`, 100+ models incl. DeepSeek) — both OpenAI-compatible so they reuse the existing branch; provider order everywhere is now groq, cerebras, nvidia, gemini, openrouter, mistral, anthropic. Wizard rewritten card-free-first with real limits, plus key sniffing for `csk-` and `nvapi-`. **Gemini billing gotcha:** free tier covers gemini-2.5-flash but NOT Pro/Flash-Lite — AI_MODELS.gemini trimmed to free-tier models only, and the wizard says so.

**v39 — world resolver, live map, Delta 1.1, hero fixes:** Copilot parsing rewritten (`cpParseRegex`): budget accepts 8k/12,000/Rs/INR/"under 15k", days from "5 day/night", destination via DB fuzzy -> preposition -> **stopword-stripped leftover token** (this is what makes "Manali 5 day plan under 8k inr" work; it previously fell through to the generic reply). **`rwResolvePlace()` = the honest "whole map database"**: an embedded world gazetteer is hundreds of MB, so any place resolves live through Open-Meteo geocoding (keyless, worldwide, includes villages), cached per-place in `rw_geo_*`. **Ranking matters** — raw API order returns fuzzy foreign matches first, so results are sorted exact-name -> home country (`RW_HOME_CC`, default IN) -> population; without this "Kufri" returned Türkiye then Pakistan instead of Himachal. Very small hamlets (Tosh, Bir) are genuinely absent from the free dataset — known limit, don't claim total coverage. Copilot output now shows admin/country/population/elevation/timezone for unknown places, adds a stays comparison line, and the plan button carries the day count into the planner (PDF lives there). **Live map** (`openMapExplorer`): Leaflet 1.9.4 + OSM tiles lazy-loaded from CDN — chosen over Google Maps/MapMyIndia because both need a billed API key; tap anywhere -> Nominatim reverse geocode + Open-Meteo current/3-day + elevation/timezone, with Plan/Things-to-do/Stays actions. Tiles are never SW-cached. UI: hero textarea ~104px with `#heroLog` moved ABOVE the box (newest answer sits right over the input), Quick-start + drawer Copilot entries call `cpFocusHero()` (inline, no popup), map tile added to Quick start, `rwPaintPhotos(row,list)` extracted so every carousel row gets real Wikimedia photos, and the 13-second phonk auto-intro is removed (speaker button still works). Built-in engine branded **"Delta 1.1"** — chip label only; the toast states plainly it is RoamWise's own data+rules engine, NOT a language model.

**v38 — hero polish, nav-intent Copilot, dynamic rows, living themes:** Hero rebuilt full-size (textarea fills the card; 34px round mic+send absolutely positioned INSIDE bottom-right; model chips bottom-left) and answers **inline** via `#heroLog` — `_cpTargetLog` switches between 'heroLog' and 'cpLog', so sending from the hero never opens the popup (drawer entry still uses the overlay). Copilot brain: NAV intent table checked first (open/go to/show + settings/store/music/profile/my trips/saved/pro/wizard/treks/explore -> runs the fn, replies "Opening X..."); `cpGoPlan(dest)` fills `destInput` + `tabGo('plan')` + `runSearch()` for BOTH known and unknown destinations; `cpDbFind` adds fuzzy 5+char prefix matching so the built-in engine gets smarter as DB grows; smart engine also answers "A vs B" comparisons and N-day budget math in $ and INR. Home rows are now month-aware: Popular-now sorts by live-event pin + bestM season match + daily seed, plus three new rows below the hero ("In season - {Month}", "Low-crowd escapes", "Easy visa for Indians") built by `miniRow()` -> all cards route through cpGoPlan. `#promofilm` moves inside `#creator` at boot (fold title "About the creator - The Film"). `.tabbar` hard-clamped (max-height 74px, overflow hidden, align-items start) - fixes the tall empty box seen on device; verified 73px. **Living themes:** `RW_THEMES` = 16 (winter/summer/monsoon/autumn/spring x day/night + dawn/dusk + holi/diwali/independence/newyear); `rwPickTheme()` checks festival windows first, then dawn 5-8 / dusk 17-20 / night 20-5, then Indian seasons; `rwApplyTheme` sets an html-level gradient and starts a fixed `#rwFx` canvas (z-index -1, pointer-events none) with per-theme particles: rain / snow / leaves / petals / stars / fireflies / confetti / tricolor / diyas / clouds / sunhaze. <=80 particles, rAF, pauses on `document.hidden`, disabled under prefers-reduced-motion, manual override key `rw_theme_mode`.

**v37 — Copilot hero + conversational engine + UI reorg:** Copilot is now the app's front door — a Claude-style input (`#copilotHero`, right after `#promoTop`) with model chips (`cpModelChips`: ⚡ Smart built-in + every BYOK provider; "+ free AI key" chip → wizard when none). Engine: full conversation with `_cpHist` (8-turn memory) — WITH a key, aiCall persona prompt answers ANY topic then appends intent-action links (`cpActionsHTML`, refactored from cpExecute); WITHOUT a key, `cpSmartAnswer` is a real knowledge engine over the destination DB (cost tiers, visa, best/quietest months) — never fakes an LLM, offers the wizard for open-ended chat. Hero enter-to-send + `copilotVoiceHero`. **UI reorg:** drawer rebuilt as 5 accordion groups (`.dr-grp/.dr-gh/.dr-gb`, `drToggle`, one-open-at-a-time — Travel/Discover/Account/App/Legal, 26 links incl. new Copilot + My Trips entries); home decluttered — ratings/store/creator wrap into `.fold-head/.fold-body` collapsibles injected at boot (scroll = video → copilot → quick start → folds). **LESSON: never write \uXXXX escapes into raw HTML — HTML doesn't parse them (drawer headers rendered literally; fixed by decoding to real chars). JS strings are fine.**

**v36 / admin v1.4 — rules-sync fix + Copilot:** ROOT CAUSE of "Sync Circle / config save / stats all broken in console but agent email works": the published Firestore rules were STALE (pasted from the admin console's embedded FULL_RULES copy, which was missing the config+crm blocks) AND/OR the admins/{uid} doc is absent — the GitHub agent bypasses rules via service account, which is why email still worked. Fixed: console's FULL_RULES is now generated from the real firestore.rules file (**RULE: any time firestore.rules changes, re-sync the embedded copy — firestore.rules must stay free of backticks/${**), and every permission failure (stats funnel, cfgLoad, cfgSave) now shows adminFixHTML(): the signed-in UID + copy button + exact steps to create admins/{uid} {ok:true}. Admin UI: real form system (.crmin styling, .fld labeled fields, .sect gold section rules, focus rings, mobile fgrid). **AI Travel Copilot** in user app (FORYOU tile, openCopilot): chat UI + Web Speech mic (guarded), two-tier parse — aiCall JSON-intent when a key exists, cpParseRegex fallback (dest via DB match or geocode w/ CITY_NAME_FIXUPS, days, ₹budget, to-city, wants[]) — actions: live 3-day forecast, work-cafes Maps link, Rome2Rio dest→to + redBus, ₹/day math, WA share, hand-off to ssOpen(); footer states bookings open partner sites. Verified with the exact Manali example sentence.

**Remote config (v35 / admin v1.3):** ALL owner-only values now live in Firestore doc **config/app** — affiliate IDs (AFF_BOOKING/AGODA/GYG/SKYSCANNER), Gumroad GUM_URL/GUM_PID, WA_NUMBER/CHANNEL/GROUP, PLAYSTORE_URL, PROMO_MP4_URL, Spotify/JioSaavn IDs, CRYPTO_WALLETS (object). Admin console **Config tab** writes it (cfgLoad/cfgSave, merge-set, blank=keep); user app has ZERO owner-fill fields left (Gumroad owner `<details>` removed from Settings; saveGumroad deleted; Gumroad values feed the old localStorage readers via config). Rules: `match /config/{doc}` public read / isAdmin write — deliberately NOT in `meta` (any-auth-writable). **Ordering trap:** applyRemoteConfig runs at DOMContentLoaded because several target vars are declared after db-init in file order — applying earlier gets overwritten by their own initializers. Cached copy in localStorage `rw_cfg` applies first (offline), then fresh fetch re-applies. `ensureWaButton()` is global+idempotent so a config-delivered WA number can create the FAB late. Deploy order: paste firestore.rules FIRST, else config reads fail (cached keeps working).

**Trip extras + CRM directory (v34):** Saved-trip view now loads live extras when online — 7-day forecast (Open-Meteo, keyless), ₹1,000-abroad FX panel (Frankfurter, **current domain api.frankfurter.dev** — the old .app 301s), and a static Safety Essentials card (112 / 1363, hospital & police map links, generic scam tips only — never invented "areas to avoid"). Coordinates geocode once and cache onto the trip record. `frankfurter` added to sw.js never-cache list. Admin console CRM tab now ships an 18-entry **starter directory** (researched Jul 2026) of orgs publicly inviting contact — Uttarakhand incubators (FIED IIM Kashipur = Tourism-focused, top lead), open-pitch angel networks (IAN Elevator Pitch, LetsVenture, 100X.VC, Mumbai Angels, Venture Catalysts, Chennai Angels), and instant-signup affiliate networks (Travelpayouts, EarnKaro, Cuelinks, Booking, Agoda). SISFS central portal **closed to new applications 15 May 2026** — don't recommend it. CSV copy: roamwise-crm-starter.csv in outputs.

**Trip Vault / affiliates / reminders / proof stamp (v32):** Saved trips live in `localStorage` key `rw_trips` (user data, not SW cache) so they read offline with zero network — `saveTripOffline()`, `openVault()`, `openVaultTrip()`. Booking links (`travelLinksHTML`) are plain public search URLs needing NO partnership or traffic minimum: Booking.com, Google Flights, Rome2Rio, GetYourGuide; affiliate IDs are optional constants (`AFF_BOOKING/AFF_AGODA/AFF_GYG`) appended only when set. Notifications are deliberately LOCAL (`notifyEnable`, `tripReminderCheck` on boot) — real push needs a server/FCM sender, which costs money, so nothing promises alerts while the app is closed. `proofStamp()` = SHA-256 fingerprint of the journey shown on the Atlas Certificate; on-chain anchoring is intentionally NOT enabled because every trustworthy chain charges gas per write. Crypto payment is direct-wallet only (`CRYPTO_WALLETS`, empty = hidden), verified like UPI UTRs — no gateway, no fees.

**PWA (as of v31):** roamwise.co.in is an installable PWA. Files in repo root: `manifest.webmanifest`, `sw.js`, `icons/` (192/512/maskable-512/apple-touch-icon 180). Service worker strategy is deliberate — **network-first for HTML** (the app is one index.html that changes every deploy; cache-first would freeze installed users on an old build with no way to push fixes) and cache-first for static assets. Firebase/ads/geocoding/YouTube and `*.mp4` are never cached. Registration is guarded by `window.isSecureContext` so it never runs inside the APK's file:// origin, and install prompts are suppressed when the RW bridge or PLAY_MODE is present. To ship a breaking SW change, bump `VERSION` in sw.js (old caches are deleted on activate).

**Ads policy (as of v21): AdSense is WEBSITE-ONLY.** The loader in index.html detects the app (window.RW bridge OR PLAY_MODE) and never loads AdSense inside any app build — AdSense-for-Content in a native app violates AdSense program policy and endangers the whole account. Play Console declarations therefore: Advertising ID = **No** (manifest verifiably has no AD_ID permission, no ad SDKs); Contains ads = **No** for the app. Do not add play-services-ads / AdMob without revisiting both declarations.

```
cd /home/claude/rw2/gradleapp
export PATH=/home/claude/rw2/gradle-8.11.1/bin:$PATH  # Gradle 8.11.1 REQUIRED for AGP 8.10.1 (SDK 36); old gradle-8.7 is deprecated
export ANDROID_HOME=/home/claude/rw2/apk/sdk
export ANDROID_SDK_ROOT=/home/claude/rw2/apk/sdk

# Sideload APK (PLAY_MODE=false):
cp /home/claude/rw2/web/index.html app/src/main/assets/index.html
gradle assembleRelease --console=plain
# → app/build/outputs/apk/release/app-release.apk

# Play Store AAB (PLAY_MODE=true):
sed 's/var PLAY_MODE = false;/var PLAY_MODE = true;/' /home/claude/rw2/web/index.html > app/src/main/assets/index.html
gradle bundleRelease --console=plain
# → app/build/outputs/bundle/release/app-release.aab
# IMPORTANT: restore the sideload assets afterward before building the APK again!
```

**If Gradle itself is lost in a future reset:** re-download `https://services.gradle.org/distributions/gradle-8.7-bin.zip`, unzip, and the `gradleapp/` project (build.gradle, settings.gradle, app/build.gradle, AndroidManifest.xml, MainActivity.java) should still be on disk since it's separate from the downloaded Gradle binary itself.

**Key files in `gradleapp/`:**
- `app/build.gradle` — dependencies, signing config (points at `../../apk/build/rw.keystore`), versionCode/versionName (bump both on every release)
- `app/src/main/java/com/gyanverse/roamwise/MainActivity.java` — now extends `AppCompatActivity` (required for `getLifecycle()`, needed by the YouTube player library) — the manifest theme MUST stay an AppCompat theme (`Theme.AppCompat.NoActionBar`) or the app will crash on launch
- `app/src/main/assets/index.html` — copy the web build here before each build (see commands above)

## Native YouTube player (fixes error 153, replaces WebView iframe entirely for the promo video)
Uses `com.pierfrancescosoffritti.androidyoutubeplayer:core:12.1.0` (the actively-maintained community standard — Google's own official player API is deprecated since 2023). JS calls `RW.playPromoVideo(videoId)`; native side shows a full-screen overlay with a real `YouTubePlayerView`, sidestepping WebView file:// origin/referrer/cookie restrictions entirely since there's no WebView-hosted iframe involved in playback anymore. On the website (no `RW` bridge), the existing plain-iframe fallback still applies unchanged.

## Current versions (update these each ship)
- Package: `com.gyanverse.roamwise`
- Last APK: v10.9 (versionCode 22, compileSdk 36, targetSdk 36, minSdk 24, R8-minified ~900KB)
- Last AAB: versionCode 22, PLAY_MODE=true, SDK 36, R8-minified with embedded deobfuscation map
- Web `index.html`: single-file architecture, now includes the RWPricing engine (see below)

## Promo video: SELF-HOSTED MP4 (final decision — do not revisit YouTube embedding)
After exhausting YouTube iframe embeds AND the native android-youtube-player library (both hit YouTube's 2025 embedder-identity restrictions — errors 152/153, confirmed as open unresolved issues industry-wide), the film now plays via a plain HTML5 `<video>` tag pointing at `https://roamwise.co.in/promo.mp4` (constant `PROMO_MP4_URL` in index.html). Direct media elements have no embed restrictions and their error events actually fire — the fallback (clean "Watch on YouTube" card) is real and verified. A small "Watch on YouTube" button always sits below the player.
**PENDING (Mohit):** upload `promo.mp4` to the GitHub repo root. Get the file via YouTube Studio → Content → your video → ⋮ → Download (official creator feature for your own uploads). If the file is >50MB, compress to 720p first (HandBrake "Fast 720p30" preset on Mac). Until uploaded, clicking Play shows the YouTube-card fallback — nothing is broken, just not inline yet.

## Pricing / business model (RWPricing engine in index.html)
Single source of truth — search `var RWPricing` in index.html. Founder offer (₹100 lifetime) closes at EITHER 1000 signups OR 365 days from `LAUNCH_DATE`, whichever first. Ongoing tiers: Free/Plus(₹99)/Pro(₹299)/Elite(₹499) monthly, with yearly discounts and tier-specific long-term one-time passes (3/5/10yr, priced separately per tier). Short-term day/week passes exist too. **These are starting numbers Mohit should sanity-check against real margins, not validated market pricing.**

## Architecture in one paragraph
Single `index.html` (web app) + a WebView-wrapped Android shell, now Gradle-built specifically to support the native YouTube player dependency. JS↔Java bridge (`window.RW`) exposes `saveCard()`, `openLastSaved()`, `openExternal()`, and now `playPromoVideo()`. Firebase (Auth + Firestore) provides accounts, Pro status, ratings, squads, the pricing/trial system. GitHub Actions agent package runs cron jobs (SEO, news, ops report, newsletter) with zero server needed.

## Old manual toolchain (legacy, kept for reference only)
```
# 1. Download Android SDK cmdline-tools + install build-tools/platform
curl -sL -o cmdline-tools.zip https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
unzip -q cmdline-tools.zip -d tmp && mkdir -p sdk/cmdline-tools && mv tmp/cmdline-tools sdk/cmdline-tools/latest
yes | sdk/cmdline-tools/latest/bin/sdkmanager --sdk_root=sdk "platform-tools" "platforms;android-34" "build-tools;34.0.0"

# 2. Install JDK if missing (javac not found = only JRE present)
apt-get update && apt-get install -y openjdk-21-jdk-headless

# 3. Download bundletool for AAB
curl -sL -o bundletool.jar https://github.com/google/bundletool/releases/download/1.15.6/bundletool-all-1.15.6.jar

# 4. Generate keystore ONLY if truly lost AND nothing published yet:
keytool -genkeypair -v -keystore build/rw.keystore -alias roamwise -keyalg RSA -keysize 2048 \
  -validity 10950 -storepass roamwise2026 -keypass roamwise2026 \
  -dname "CN=Mohit Pandey, OU=GYANVERSE, O=RoamWise, L=Almora, ST=Uttarakhand, C=IN"
```
**AndroidManifest.xml MUST include** `<uses-sdk android:minSdkVersion="24" android:targetSdkVersion="34" />` — omitting this caused a real bug once (Android blocked install as "incompatible/unsafe").

## Firebase / Firestore
- Full security rules live in `firestore.rules` in this same repo folder — paste directly into Firebase Console → Firestore → Rules.
- Collections in use: `users` (+`devices` subcollection), `claims`, `payments`, `admins`, `pulse`, `requests`, `stats`, `ratings`, `squads`, `reports`.
- Pro is **account-bound, not device-bound** — verified fix for a real leak where logout didn't clear Pro (stale Firestore listener wasn't unsubscribed).

## Constants still waiting on Mohit (search `index.html` for these)
- `ADSENSE_ID` — ✅ set (`ca-pub-4943859484482348`)
- `AFF_BOOKING` — empty, waiting on a Booking.com/Agoda affiliate ID
- `PLAYSTORE_URL` — empty, fill in once the app is live on Play Store (unlocks in-app "rate us" nudges)
- Book links — ✅ all wired (Amazon/Pothi/Flipkart, real URLs)
- Spotify/JioSaavn — ✅ wired (`SPOTIFY_ARTIST_ID`, `SPOTIFY_PLAYLIST_ID`, `JIOSAAVN_URL`)
- `WA_NUMBER` / `WA_CHANNEL` / `WA_GROUP` — still empty, waiting on Mohit to create these himself (can't be done by an agent — needs his phone/SIM)

## Known-fixed bugs (don't reintroduce)
1. **YouTube embed error 153 inside the app**: fixed by using the real YT IFrame Player API with `onStateChange` verification (checking for actual `PLAYING` state, not just `onReady`) — `onReady` can fire while YouTube silently shows its own in-frame error with no JS `onError` event. Falls back to native `RW.openExternal()` after ~4.5s if PLAYING never arrives.
2. **Pro survives logout**: was caused by an unsubscribed Firestore `onSnapshot` listener from a prior session firing a stale cached event after logout. Fixed by capturing and explicitly unsubscribing on sign-out, and making the signed-out auth branch authoritative.
3. **Missing `<uses-sdk>`**: causes "app not compatible" install failures on modern Android — always verify with `aapt2 dump badging <apk> | grep sdkVersion` after any manifest rebuild.
4. **Popular-card photo loading**: must be sequential (not parallel/burst), or the weserv.nl proxy rate-limits and some images silently never load.
5. **Journey Card canvas tainting**: any photo drawn onto the exportable card canvas must go through `fetchImg64()` (fetch→blob→dataURL), never a direct cross-origin `Image.src` — otherwise `toDataURL()` throws on save/share.

## Skills-check reminder for future sessions
Always re-read this file, then check `/mnt/user-data/outputs/` for the latest shipped files before assuming anything is missing.
