{
  "name": "roamwise",
  "version": "1.0.0",
  "description": "RoamWise Pro — AI travel planner (Capacitor Android wrapper)",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint js app.js",
    "check": "node --check app.js && node --check rw-config.js && node --check platform-v5/audio-only.js && node --check platform-v5/atlas-shinobi.js && node --check js/audio/focus.js && node --check js/audio/cues.js && node --check js/runtime/freshness.js && node --check js/voice/tusk-speak.js && node --check roamwise-premium-itinerary.js && node tools/check-line-limits.js && npm run typecheck",
    "index": "node tools/generate-function-index.js",
    "index:css": "node tools/generate-css-index.js",
    "index:all": "npm run index && npm run index:css",
    "mod-status": "node tools/modularization-status.js",
    "evals": "node tools/run-evals.js",
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor-community/speech-recognition": "^6.0.0",
    "@capacitor/android": "^6.1.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/geolocation": "^6.0.1",
    "@capacitor/haptics": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "typescript": "^7.0.2"
  }
}
